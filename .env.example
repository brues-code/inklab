# MySQL/MariaDB connection for development data imports (tw_world world DB).
# Copy to .env and fill in your local credentials. Not needed for release
# builds — those use the embedded SQLite database.
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=changeme
MYSQL_DATABASE=tw_world
AOWOW_DATABASE=aowow
